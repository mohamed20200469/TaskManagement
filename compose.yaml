services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=taskmanagement'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=idris'
    ports:
      - '5432:5432'
